//Object

const person = {
    name: "Thiago",
    age: 25,
    weight: 65,
    isAdmin: true
}
console.log(`${person.name} tem ${person.age} anos`)
/*Deve sempre buscar as propriedades usando o nome declarado no objeto e a propriedade separando por pontos*/